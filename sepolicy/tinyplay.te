type tinyplay, domain;
type tinyplay_exec, exec_type, file_type;
init_daemon_domain(tinyplay);

allow tinyplay audio_device:chr_file { ioctl read open write };
allow tinyplay audio_device:dir { search };
